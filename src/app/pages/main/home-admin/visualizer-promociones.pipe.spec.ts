import { VisualizerPromocionesPipe } from './visualizer-promociones.pipe';

describe('VisualizerPromocionesPipe', () => {
  it('create an instance', () => {
    const pipe = new VisualizerPromocionesPipe();
    expect(pipe).toBeTruthy();
  });
});
